- name: Update FreeIPA Masters
  hosts: pve_ipa
  vars:
    ansible_user: svc-ansible-infra
    ansible_ssh_pass: "{{ vault_ipa_service_accounts.svc_ansible_infra.password }}"
    ansible_become_pass: "{{ vault_ipa_service_accounts.svc_ansible_infra.password }}"
  gather_facts: true 
  become: true
  tasks:
    - name: Update host
      ansible.builtin.dnf:
        name: "*"
        state: latest
        update_cache: true
      become: true

- name: Update IPA joined hosts
  hosts: pve_ipajoined
  vars:
    #ansible_user: svc-ansible-infra
    #ansible_ssh_pass: "{{ vault_ipa_service_accounts.svc_ansible_infra.password }}"
    #ansible_become_pass: "{{ vault_ipa_service_accounts.svc_ansible_infra.password }}"
  gather_facts: true 
  become: true
  tasks:
    - name: Update host
      ansible.builtin.dnf:
        name: "*"
        state: latest
        update_cache: true
      become: true

